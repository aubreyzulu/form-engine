# Local Postgres defaults used by docker-compose.yml.
POSTGRES_USER=app
POSTGRES_PASSWORD=app
POSTGRES_DB=form-engine
POSTGRES_PORT=5432

# API / Prisma. Replace DATABASE_URL before running migrations against a real DB.
DATABASE_URL="postgresql://aubreyzulu@localhost:5432/form-engine"
PORT=4000
CORS_ORIGINS="http://localhost:3000"

# Web.
NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1

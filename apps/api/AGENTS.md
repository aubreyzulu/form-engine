# AGENTS.md — apps/api

This file is a Codex compatibility entry point. The source of truth for this
folder is `CLAUDE.md`. Read `./CLAUDE.md` before touching backend code; if this
file and `CLAUDE.md` ever disagree, follow `CLAUDE.md`.

Also read the root `../../CLAUDE.md` for monorepo-wide rules before making
changes here.
